// calculate the percentage

var mk= 60
var tmk = 100
var prct = (mk / tmk) * 100
console.log(prct)


function calcper(mk, tmk) {
    var per = (mk/ tmk) *100;
    return per
}

var totalper= calcper(53 , 60)
console.log(totalper)
